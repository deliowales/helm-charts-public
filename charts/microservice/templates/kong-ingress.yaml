{{- if eq .Values.kongIngress.enabled true }}
apiVersion: configuration.konghq.com/v1
kind: KongIngress
metadata:
  name: do-not-preserve-host
  labels:
    {{- include "microservice.labels" . | nindent 4 }}
  annotations:
    {{- include "microservice.annotations" . | nindent 4 }}
route:
  preserve_host: false
upstream:
  host_header: {{ include "microservice.application.name" . }}.{{ .Release.Namespace }}.svc
{{- end }}